# 20260717_xsim_smoke_test 真实 XSim 验证报告

## AI 模型类型

- Codex / GPT-5 coding agent。

## 本轮任务

- 用户授权进行真实 XSim 冒烟验证。
- 目标命令：`./scripts/vivado2018_common.sh xsim-smoke`。
- 本轮允许调用 Vivado/XSim；仍不执行综合、实现、bitstream、上板或 git 写操作。

## git baseline

- branch：`dev`。
- 开工 `git status --short`：
  - `M ai_workflow/HANDOFF_CURRENT.md`
  - `M ai_workflow/TASK_INDEX.md`
  - `M scripts/vivado2018_common.sh`
  - `?? ai_workflow/tasks/20260717_xsim_smoke_test/`
  - `?? constraints/PIN.xdc`
  - `?? docs/xilinx_fpga_multiboot.drawio`
  - `?? prompts/codex/`
  - `?? rtl/hdl/user/Top.v`
  - `?? rtl/hdl/user/multiboot/`
  - `?? rtl/hdl/user/xsim_smoke_dut.v`
  - `?? scripts/open_latest_gui.sh`
  - `?? sim/tb/tb_multiboot_ctrl.v`
  - `?? sim/tb/tb_xsim_smoke.v`
  - `?? sim/wave/`
  - `?? sim/xsim/`
  - `?? tcl/sim/`
- 开工 `git diff --stat`：
  - `ai_workflow/HANDOFF_CURRENT.md | 12 +++++++-----`
  - `ai_workflow/TASK_INDEX.md | 9 ++++++---`
  - `scripts/vivado2018_common.sh | 6 +++++-`
- recent log：`f8eb58d first commit`。

## 输入事实摘要

- `TASK_STATE.md` 显示唯一下一步为授权并执行真实 XSim 冒烟测试。
- 用户本轮已授权真实 XSim 冒烟验证。
- 本轮仍保留证据边界：smoke PASS 只覆盖最小 DUT/TB，不覆盖 Multiboot 功能和上板行为。

## latest report 发现结果

- 本轮执行 `find reports -maxdepth 1 -type f | sort | tail -n 20` 时，只发现 `reports/.gitkeep`。
- `HANDOFF_CURRENT.md` 开工时指向 `reports/20260717_xsim_smoke_test_20260717_010037.md`，但该文件在 `reports/` 当前目录发现结果中不存在。
- 本轮已将最新 report 指针更新为：`reports/20260717_xsim_smoke_test_20260717_011642.md`。

## 边界自检

- WRITE_ALLOW / ALLOWLIST：本轮用户授权真实 XSim 验证；为记录验证结果，更新当前任务状态文件、handoff、任务索引和本报告；为修复首次运行暴露的 Tcl 调用问题，修改 `tcl/sim/xsim_smoke.tcl`。
- RUN_POLICY：允许运行 `./scripts/vivado2018_common.sh xsim-smoke`；未运行综合、实现、bitstream；未上板。
- 实际运行命令：
  - `git branch --show-current`
  - `git status --short`
  - `git diff --stat`
  - `./scripts/vivado2018_common.sh xsim-smoke`
  - `bash -n scripts/vivado2018_common.sh`
  - `git diff --check`
  - `sed -n '1,130p' tcl/sim/xsim_smoke.tcl`
  - `./scripts/vivado2018_common.sh xsim-smoke`
  - `sed -n '1,160p' _runs/latest/summary.txt`
  - `sed -n '1,180p' _runs/latest/status.txt`
  - `rg -n "RESULT=|ERROR|CRITICAL WARNING|CRITICAL|WARNING|Built simulation snapshot" ...`
  - `find _artifacts/common_vivado/20260717_011607_xsim-smoke -maxdepth 1 -type f -printf '%p %s bytes\n'`
  - `readlink _runs/latest`
- 是否触碰禁止项：no。
- 是否修改 hard-readonly 文件：no。
- 是否越界：no。

## 修改摘要

- 修复 `tcl/sim/xsim_smoke.tcl`：将 `xvlog`、`xelab`、`xsim` 从 Vivado Tcl 内建命令调用方式改为 Tcl `exec` 外部命令调用。
- 更新 `TASK_STATE.md`、`HANDOFF_CURRENT.md`、`TASK_INDEX.md`，记录真实运行结果与下一步。
- 新增本报告。

## 实际改动文件

- `tcl/sim/xsim_smoke.tcl`
- `ai_workflow/tasks/20260717_xsim_smoke_test/TASK_STATE.md`
- `ai_workflow/HANDOFF_CURRENT.md`
- `ai_workflow/TASK_INDEX.md`
- `reports/20260717_xsim_smoke_test_20260717_011642.md`

## 真实运行结果

### 第一次运行

- 命令：`./scripts/vivado2018_common.sh xsim-smoke`
- run id：`20260717_011503_xsim-smoke`
- 结果：FAILED
- 原因：Vivado Tcl 报 `invalid command name "xvlog"`；原 Tcl 将 `xvlog` 当作 Vivado Tcl 内建命令调用。
- 处置：将 XSim 三步改为 Tcl `exec` 外部命令调用。

### 第二次运行

- 命令：`./scripts/vivado2018_common.sh xsim-smoke`
- run id：`20260717_011607_xsim-smoke`
- 结果：SUCCESS
- `_runs/latest` 指向：`common_vivado/20260717_011607_xsim-smoke`
- `summary.txt`：
  - `exit_status: 0`
  - `result: SUCCESS`
- `status.txt`：
  - `exit_status=0`
  - `result=SUCCESS`
- `xsim_smoke_xelab.log`：
  - `Built simulation snapshot xsim_smoke_snapshot`
- `xsim_smoke_xsim.log`：
  - `RESULT=PASS`
- WDB：
  - `_artifacts/common_vivado/20260717_011607_xsim-smoke/xsim_smoke.wdb`
  - size：9879 bytes

## PASS/FAIL 判断

- 本轮存在真实 XSim 运行证据：`_runs/common_vivado/20260717_011607_xsim-smoke/logs/xsim_smoke_xsim.log`。
- 该日志含 `RESULT=PASS`。
- `rg` 检查未在三份 XSim 日志中发现 `ERROR`、`CRITICAL` 或 `WARNING` 匹配项。
- 因此可以声称：最小 XSim 冒烟测试已 PASS。

## WDB 查看方法

- 本轮已生成 WDB：
  - `_artifacts/common_vivado/20260717_011607_xsim-smoke/xsim_smoke.wdb`
- 可在 Vivado GUI 中打开该 `.wdb` 查看波形。

## 额外自主修改说明

- 做了什么：修复 `tcl/sim/xsim_smoke.tcl` 中的 XSim 命令调用方式。
- 为什么做：首次真实运行证明 Vivado 2018.3 batch Tcl 中 `xvlog` 不是内建命令，必须通过外部命令执行。
- 收益：统一 Shell 入口现在能实际跑通 `xvlog -> xelab -> xsim -> RESULT=PASS`。
- 风险：仅影响 `xsim-smoke` 仿真入口，不影响 create/synth/impl/bit/full 构建分支。
- 是否影响默认行为：no。
- 是否影响 legacy path：no。
- 人工 review 重点：确认 `run_external_cmd` 的 `exec {*}$cmd_list` 在目标 Vivado/Tcl 环境中可接受；本轮实际运行已验证通过。

## 验证情况

已通过：

```bash
bash -n scripts/vivado2018_common.sh
git diff --check
./scripts/vivado2018_common.sh xsim-smoke
```

真实 XSim PASS 证据：

```text
_runs/common_vivado/20260717_011607_xsim-smoke/logs/xsim_smoke_xsim.log:19:RESULT=PASS
```

WDB 证据：

```text
_artifacts/common_vivado/20260717_011607_xsim-smoke/xsim_smoke.wdb 9879 bytes
```

## 未执行事项

- 未运行综合。
- 未运行实现。
- 未生成 bitstream。
- 未上板。
- 未验证 Multiboot 功能正确性。
- 未验证任何真实硬件行为。

## 风险与注意事项

- `scripts/vivado2018_common.sh` 开工前已有 part name usage 脏改动，本轮未回滚。
- 仍存在本轮无关未跟踪文件：`constraints/PIN.xdc`、`docs/xilinx_fpga_multiboot.drawio`、`rtl/hdl/user/Top.v`、`rtl/hdl/user/multiboot/`、`scripts/open_latest_gui.sh`、`sim/tb/tb_multiboot_ctrl.v` 等；本轮未清理。
- 本轮 PASS 只覆盖最小 smoke DUT/TB，不代表 Multiboot 功能通过。

## 下一轮建议

- 人工审查本任务 diff/report 后决定是否提交。
- 若继续技术推进，另起一轮定义 Multiboot 功能级仿真目标和验收口径。

## 待授权事项

- 若要继续验证 Multiboot 功能，需要新授权：允许新增/修改对应 RTL/TB/Tcl，并定义功能级 PASS/FAIL 判据。

## FPGA 调试 yes/no 状态

- 是否修改 RTL：no，本轮未改 RTL；上一轮新增 smoke DUT 保持不变。
- 是否修改 XDC：no。
- 是否修改 Tcl：yes，修复 `tcl/sim/xsim_smoke.tcl` 的 XSim 命令调用方式。
- 是否修改 Python：no。
- 是否运行 Vivado：yes，用户授权真实 XSim 冒烟验证；命令为 `./scripts/vivado2018_common.sh xsim-smoke`。
- 是否生成 bitstream：no。
- 是否上板：no。
- 是否发送真实 UDP 包：no。
- 是否修改 MIG/IP：no。

## 最终 git status --short

```text
 M ai_workflow/HANDOFF_CURRENT.md
 M ai_workflow/TASK_INDEX.md
 M scripts/vivado2018_common.sh
?? ai_workflow/tasks/20260717_xsim_smoke_test/
?? constraints/PIN.xdc
?? docs/xilinx_fpga_multiboot.drawio
?? prompts/codex/
?? reports/20260717_xsim_smoke_test_20260717_011642.md
?? rtl/hdl/user/Top.v
?? rtl/hdl/user/multiboot/
?? rtl/hdl/user/xsim_smoke_dut.v
?? scripts/open_latest_gui.sh
?? sim/tb/tb_multiboot_ctrl.v
?? sim/tb/tb_xsim_smoke.v
?? sim/wave/
?? sim/xsim/
?? tcl/sim/
```

## 最终 git diff --stat

普通 `git diff --stat` 不显示未跟踪文件；未跟踪文件见 `git status --short`。

```text
 ai_workflow/HANDOFF_CURRENT.md | 14 +++++++++-----
 ai_workflow/TASK_INDEX.md      |  9 ++++++---
 scripts/vivado2018_common.sh   |  6 +++++-
 3 files changed, 20 insertions(+), 9 deletions(-)
```
